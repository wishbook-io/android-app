package com.wishbook.catalog;import android.os.Handler;import androidx.appcompat.app.AppCompatActivity;import android.widget.Toast;import com.wishbook.catalog.home.Activity_Home;/** * Created by vigneshkarnika on 06/03/16. */public class BaseActivity extends AppCompatActivity {    boolean doubleBackToExitPressedOnce = false;    @Override    public void onBackPressed() {//        if (getSupportFragmentManager().getBackStackEntryCount() > 0) {//            getSupportFragmentManager().popBackStackImmediate();//        } else {            if (doubleBackToExitPressedOnce) {                finish();                //super.onBackPressed();            } else {                this.doubleBackToExitPressedOnce = true;                try {                    Toast.makeText(this, "Please click BACK again to exit", Toast.LENGTH_SHORT).show();/*                    if (Activity_Home.tabs != null) {                        Activity_Home.tabs.setVisibility(View.VISIBLE);                    }*/                    //Activity_Home.tabs.getTabAt(0).select();                    Activity_Home.bottomBar.selectTabAtPosition(0);                }                catch(Exception e)                {                }                new Handler().postDelayed(new Runnable() {                    @Override                    public void run() {                        doubleBackToExitPressedOnce = false;                    }                }, 2000);            }    }}